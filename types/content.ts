export type PreviewItem = {
  eyebrow: string;
  title: string;
  description: string;
  href: string;
  image: string;
  accent?: string;
};

export type ContactFormValues = {
  name: string;
  email: string;
  phone: string;
  message: string;
};
